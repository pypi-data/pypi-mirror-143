[flake8]
max-line-length = 140
exclude = .tox,.eggs,ci/templates,build,dist
ignore = E203

[tool:pytest]
norecursedirs = 
	migrations
python_files = 
	test_*.py
	*_test.py
	tests.py
addopts = 
	-ra
	--strict-markers
	--doctest-modules
	--doctest-glob=\*.rst
	--tb=short
testpaths = 
	tests
markers = 
	unit: regular tests
	func: pita tests

[tool:isort]
force_single_line = True
line_length = 120
known_first_party = holdup
default_section = THIRDPARTY
forced_separate = test_holdup
skip = .tox,.eggs,ci/templates,build,dist

[egg_info]
tag_build = 
tag_date = 0

