---

  - hosts: localhost
    become: yes
    become_user: root
    tasks:
    - name: Create user
      user:
        name: '{{ neo_user }}'
        generate_ssh_key: yes

    - name: Set home folder perms
      file:
        path: '{{ user_directory }}'
        mode: 'o+rx'
        owner: '{{ neo_user }}'
        group: '{{ neo_user }}'
        recurse: yes

    - name: Set /tmp/.neocli folder perms
      file:
        path: '{{ repo_path }}'
        owner: '{{ neo_user }}'
        group: '{{ neo_user }}'
        recurse: yes

    - name: Change default shell to bash
      shell: "chsh {{ neo_user }} -s $(which bash)"
...
