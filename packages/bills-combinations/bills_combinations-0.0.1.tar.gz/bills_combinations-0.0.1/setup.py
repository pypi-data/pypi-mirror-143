from setuptools import setup

setup(
    name="bills_combinations",
    version="0.0.1",
    description="count of possible combinations of bills in a wallet for a specific amount",
    py_modules=["bills_combinations"],
    package_dir={"": "src"},
    )
