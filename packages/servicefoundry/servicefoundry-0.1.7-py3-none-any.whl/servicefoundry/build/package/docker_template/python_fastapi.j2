FROM {{ python_version }}

COPY . ./app

{% if requirement_txt_file %}
COPY {{ requirement_txt_file }} ./requirements.txt

RUN pip install -r ./requirements.txt
{% endif %}

RUN pip install uvicorn

EXPOSE {{ port }}

WORKDIR app

ENTRYPOINT uvicorn {{service}}:app --host 0.0.0.0 --port {{ port }}
