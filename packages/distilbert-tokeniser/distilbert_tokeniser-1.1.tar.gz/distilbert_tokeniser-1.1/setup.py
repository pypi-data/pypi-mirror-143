from setuptools import setup, find_packages


setup(
    name='distilbert_tokeniser',
    version='1.1',
    license='',
    author="",
    platforms=["any"],
    author_email='email@example.com',
    url='',
    packages=find_packages(exclude=["test"]),
    keywords='distilbert_tokeniser',
    install_requires=[ ],

)
